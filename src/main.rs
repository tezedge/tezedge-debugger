pub mod actors;
pub mod utility;
pub mod storage;

// use riker::actors::*;
// use riker_producer::prelude::*;
// use crate::actors::producers::nfqueue_producer::{PacketProducer, PacketOrchestrator};

fn main() -> Result<(), std::io::Error> {
    // let sys = ActorSystem::new()
    //     .expect("failed to create actor system");
    // let _ = sys.actor_of_args::<Producer<PacketProducer, PacketOrchestrator>, _>("producer", ((), ()));
    // loop {}
    Ok(())
}
