{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "compose-up-rust",
            "command": "docker-compose -f docker-compose.rust.yml up -d --build",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": [
                "$rustc"
            ]
        }
    ]
}